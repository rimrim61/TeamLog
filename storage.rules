rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    match /teamFiles/{teamId}/{fileId}/{fileName} {
      allow read: if request.auth != null;
      allow write: if request.auth != null
                   && request.resource.size <= 20 * 1024 * 1024
                   && request.resource.contentType.matches(
                     'application/pdf|application/msword|application/vnd.openxmlformats-officedocument.*|application/vnd.ms-powerpoint|application/zip|image/jpeg|image/png|application/octet-stream'
                   );
    }
  }
}
